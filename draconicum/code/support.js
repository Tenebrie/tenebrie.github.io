
//=====================================================================
// Find an element by id
//=====================================================================
function Find(Id) {
	return document.getElementById(Id);
}

//=====================================================================
// Get an arbitrary random value
//=====================================================================
function GetRandom(min, max) {
	return (Math.random() * (max - min)) + min;
}